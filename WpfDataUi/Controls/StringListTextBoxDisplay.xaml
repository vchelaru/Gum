<UserControl
    x:Class="WpfDataUi.Controls.StringListTextBoxDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:WpfDataUi.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfDataUi.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    MinHeight="150"
    d:DesignWidth="200"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:AddDoubleConverter x:Key="AddDoubleConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="150" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            x:Name="Label"
            MinWidth="100"
            Padding="4,4,4,0"
            VerticalAlignment="Center"
            TextWrapping="Wrap">
            Property Label:
        </TextBlock>
        <TextBox
            x:Name="TextBox"
            Grid.Row="1"
            MinWidth="60"
            VerticalContentAlignment="Top"
            AcceptsReturn="True"
            GotFocus="TextBox_GotFocus"
            LostFocus="TextBox_LostFocus"
            TextChanged="TextBox_TextChanged"
            TextWrapping="NoWrap"
            VerticalScrollBarVisibility="Auto">
            <TextBox.ContextMenu>
                <ContextMenu />
            </TextBox.ContextMenu>
        </TextBox>
        <TextBlock
            x:Name="HintTextBlock"
            Grid.Row="2"
            Margin="0,0,-4,0"
            Padding="8,0,0,0"
            FontSize="{Binding Path=FontSize, RelativeSource={RelativeSource AncestorType={x:Type local:StringListTextBoxDisplay}}, Converter={StaticResource AddDoubleConverter}, ConverterParameter=-2}"
            TextWrapping="Wrap" />
    </Grid>
</UserControl>
