<UserControl
    x:Class="WpfDataUi.Controls.FileSelectionDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:WpfDataUi.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfDataUi.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:AddDoubleConverter x:Key="AddDoubleConverter" />
    </UserControl.Resources>
    <StackPanel>

        <Grid x:Name="TopRowGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Name="FirstColumn" Width="{Binding FirstGridLength}" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                x:Name="Label"
                MinWidth="100"
                Padding="4"
                VerticalAlignment="Center"
                TextWrapping="Wrap">
                Property Label:
                <TextBlock.ContextMenu>
                    <ContextMenu />
                </TextBlock.ContextMenu>
            </TextBlock>

            <TextBox
                x:Name="TextBox"
                Grid.Column="1"
                MinWidth="60"
                Margin="0,0,51,0"
                VerticalAlignment="Center"
                LostFocus="TextBox_LostFocus_1">
                <TextBox.ContextMenu>
                    <ContextMenu />
                </TextBox.ContextMenu>
            </TextBox>
            <StackPanel
                Grid.Column="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    Width="{Binding ActualHeight, ElementName=TextBox}"
                    Height="{Binding ActualHeight, ElementName=TextBox}"
                    MinWidth="24"
                    MinHeight="24"
                    VerticalAlignment="Center"
                    Click="Button_Click_1">
                    ...
                </Button>
                <Button
                    Grid.Column="2"
                    Width="{Binding ActualHeight, ElementName=TextBox}"
                    Height="{Binding ActualHeight, ElementName=TextBox}"
                    MinWidth="24"
                    MinHeight="24"
                    Margin="1,0,0,0"
                    Click="ViewInExplorerClicked">
                    <Image Source="/WpfDataUi;component/Content/Icons/OpenFolder.png" />
                </Button>

            </StackPanel>

        </Grid>
        <TextBlock
            x:Name="HintTextBlock"
            Grid.Row="2"
            Grid.ColumnSpan="3"
            MaxWidth="{Binding ActualWidth, ElementName=TopRowGrid}"
            Margin="0,0,-4,0"
            Padding="8,1,0,4"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            FontSize="{Binding Path=FontSize, RelativeSource={RelativeSource AncestorType={x:Type local:FileSelectionDisplay}}, Converter={StaticResource AddDoubleConverter}, ConverterParameter=-2}"
            Text="This is a property description. But what if it is longer?"
            TextWrapping="Wrap" />
    </StackPanel>
</UserControl>
