<UserControl
    x:Class="WpfDataUi.Controls.SliderDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:WpfDataUi.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfDataUi.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <conv:AddDoubleConverter x:Key="AddDoubleConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="TopRowGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Name="FirstColumn" Width="{Binding FirstGridLength}" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="65" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock
                x:Name="Label"
                Grid.RowSpan="2"
                MinWidth="100"
                Padding="4"
                VerticalAlignment="Top"
                TextWrapping="Wrap">
                Property Label:
            </TextBlock>

            <Slider
                x:Name="Slider"
                Grid.Column="1"
                MinWidth="60"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                PreviewMouseLeftButtonUp="Slider_MouseLeftButtonUp"
                Thumb.DragCompleted="Slider_DragCompleted"
                ValueChanged="Slider_ValueChanged" />

            <TextBox
                x:Name="TextBox"
                Grid.Column="2"
                Margin="3,1,1,1"
                LostFocus="TextBox_LostFocus_1">
                <TextBox.ContextMenu>
                    <ContextMenu />
                </TextBox.ContextMenu>
            </TextBox>
            <Grid Grid.Row="1" Grid.Column="1">
                <!--
                    Negative Y margin to make the text show closer to the slider. The overlap looks bad
                    here in the editor, but it's fine in my testing in the actual app.
                -->
                <TextBlock
                    x:Name="MinValueText"
                    Margin="0,-4,0,0"
                    VerticalAlignment="Top"
                    IsHitTestVisible="False">
                    0
                </TextBlock>
                <TextBlock
                    x:Name="MaxValueText"
                    Margin="0,-4,0,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    IsHitTestVisible="False">
                    100
                </TextBlock>
            </Grid>
        </Grid>

        <TextBlock
            x:Name="HintTextBlock"
            Grid.Row="1"
            MaxWidth="{Binding ActualWidth, ElementName=TopRowGrid}"
            Margin="0,0,-4,0"
            Padding="8,1,0,4"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            FontSize="{Binding Path=FontSize, RelativeSource={RelativeSource AncestorType={x:Type local:SliderDisplay}}, Converter={StaticResource AddDoubleConverter}, ConverterParameter=-2}"
            TextWrapping="Wrap">
            This is the bottommost text
        </TextBlock>
    </Grid>
</UserControl>
