# Convert scaffold

Internal Gum project template used by `convert.ts` when emitting a standalone `.gumx` (CLI or plugin staging).

Not an HTML sample — do not open via Import HTML. Fixture HTML lives in `../samples/`.
