{
  "name": "html-to-gum-fidelity",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Site/bookmark pixel-gate harness for HtmlToGum (not the convert pipeline)",
  "scripts": {
    "site-fidelity": "tsx site-fidelity.ts",
    "bookmark-fidelity": "python bookmark_fidelity_batch.py seeds.json",
    "test": "tsx --test *.test.ts"
  },
  "dependencies": {
    "playwright-core": "1.61.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4"
  }
}
