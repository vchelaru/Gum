<UserControl
  x:Class="Gum.Plugins.InternalPlugins.Output.MainOutputPluginView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:behaviors="clr-namespace:Gum.Behaviors"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:managers="clr-namespace:Gum.Managers"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:wpf="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
  d:DataContext="{d:DesignInstance Type=managers:MainOutputViewModel,
                                   IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Button
      Margin="0,0,0,4"
      HorizontalAlignment="Left"
      HorizontalContentAlignment="Left"
      Command="{Binding ClearOutputCommand}"
      Content="{wpf:FluentIcon Icon=TextGrammarDismiss}"
      Style="{StaticResource IconButton}"
      ToolTip="Clear Output" />
    <TextBox
      x:Name="OutputTextBox"
      Grid.Row="1"
      HorizontalAlignment="Stretch"
      VerticalAlignment="Stretch"
      VerticalContentAlignment="Top"
      behaviors:TextBoxAutoScroll.AutoScrollToEnd="True"
      Style="{DynamicResource Frb.Styles.Textbox.Readonly}"
      TabIndex="0"
      Text="{Binding OutputText, Mode=OneWay}"
      TextWrapping="Wrap"
      VerticalScrollBarVisibility="Visible" />
  </Grid>
</UserControl>
