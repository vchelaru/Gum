<UserControl
  x:Class="Gum.Services.Dialogs.ChoiceDialogView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:converters="clr-namespace:Gum.Converters"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Gum.Services.Dialogs"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  d:DesignHeight="450"
  d:DesignWidth="800"
  Width="350"
  local:Dialog.DialogTitle="{Binding Title}"
  mc:Ignorable="d">
  <UserControl.Resources>
    <converters:AreEqualMultiConverter x:Key="AreEqualMultiConverter" />
  </UserControl.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock
      Grid.Row="0"
      Margin="0,0,0,16"
      Text="{Binding Message}"
      TextWrapping="Wrap" />
    <ItemsControl
      Grid.Row="1"
      ItemsSource="{Binding OptionValues}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <StackPanel />
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>

      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <RadioButton
            Margin="0,6,0,0"
            Checked="ToggleButton_OnChecked"
            Content="{Binding}"
            GroupName="OptionsGroup">
            <RadioButton.ContentTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding}" TextWrapping="Wrap" />
              </DataTemplate>
            </RadioButton.ContentTemplate>
            <RadioButton.IsChecked>
              <MultiBinding Converter="{StaticResource AreEqualMultiConverter}" Mode="OneWay">
                <Binding Path="DataContext.SelectedValue" RelativeSource="{RelativeSource FindAncestor, AncestorType=ItemsControl}" />
                <Binding Path="Content" RelativeSource="{RelativeSource Self}" />
              </MultiBinding>
            </RadioButton.IsChecked>
          </RadioButton>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </Grid>
</UserControl>
