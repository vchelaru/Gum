<controlzex:WindowChromeWindow
  x:Class="Gum.Services.Dialogs.DialogWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controlzex="urn:controlzex"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Gum.Services.Dialogs"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  x:Name="Root"
  Title="{Binding (local:Dialog.DialogTitle), ElementName=DialogImpl}"
  AllowsTransparency="True"
  Background="Transparent"
  ResizeMode="CanResize"
  SizeToContent="WidthAndHeight"
  SnapsToDevicePixels="True"
  UseLayoutRounding="True"
  WindowStartupLocation="CenterOwner"
  WindowStyle="None"
  mc:Ignorable="d">
  <controlzex:WindowChromeWindow.Resources>
    <Style TargetType="{x:Type local:Dialog}">
      <Setter Property="Content" Value="{Binding}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type local:Dialog}">
            <Border Padding="12">
              <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                <Grid.RowDefinitions>
                  <RowDefinition Height="*" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ScrollViewer
                  Grid.Row="0"
                  VerticalAlignment="Stretch"
                  HorizontalContentAlignment="Stretch"
                  VerticalContentAlignment="Stretch"
                  HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
                  <Grid>
                    <ContentPresenter x:Name="DialogPresenter" />
                  </Grid>
                </ScrollViewer>
                <ContentPresenter
                  x:Name="ActionsPresenter"
                  Grid.Row="1"
                  Margin="0,16,0,0"
                  Content="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(local:Dialog.Actions)}" />
              </Grid>
            </Border>

            <ControlTemplate.Triggers>
              <Trigger Property="local:Dialog.Actions" Value="{x:Null}">
                <Setter TargetName="ActionsPresenter" Property="Content" Value="{Binding}" />
                <Setter TargetName="ActionsPresenter" Property="ContentTemplate">
                  <Setter.Value>
                    <DataTemplate DataType="{x:Type local:DialogViewModel}">
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="*" />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ContentPresenter
                          Grid.Column="0"
                          HorizontalAlignment="Left"
                          Content="{Binding Path=(local:Dialog.AuxiliaryActions), RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:Dialog}}}">
                          <ContentPresenter.Resources>
                            <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="{x:Type Button}">
                              <Setter Property="Margin" Value="0,0,5,0" />
                              <Setter Property="Padding" Value="16,4" />
                              <Setter Property="MinWidth" Value="64" />
                            </Style>
                          </ContentPresenter.Resources>
                        </ContentPresenter>
                        <StackPanel
                          Grid.Column="1"
                          HorizontalAlignment="Right"
                          VerticalAlignment="Center"
                          Orientation="Horizontal">
                          <Button
                            MinWidth="64"
                            Margin="5,0,0,0"
                            Padding="16,4"
                            Command="{Binding AffirmativeCommand}"
                            Content="{Binding AffirmativeText}"
                            IsDefault="True" />
                          <Button
                            MinWidth="64"
                            Margin="5,0,0,0"
                            Padding="16,4"
                            Command="{Binding NegativeCommand}"
                            Content="{Binding NegativeText}">
                            <Button.Style>
                              <Style BasedOn="{StaticResource {x:Type Button}}" TargetType="{x:Type Button}">
                                <Style.Triggers>
                                  <DataTrigger Binding="{Binding NegativeText}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                  </DataTrigger>
                                </Style.Triggers>
                              </Style>
                            </Button.Style>
                          </Button>
                        </StackPanel>
                      </Grid>

                    </DataTemplate>
                  </Setter.Value>
                </Setter>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </controlzex:WindowChromeWindow.Resources>


  <Border
    Background="{DynamicResource Frb.Surface01}"
    BorderBrush="{DynamicResource Frb.Brushes.Border}"
    BorderThickness="1"
    CornerRadius="8"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True">
    <Border>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="24" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
          HorizontalAlignment="Stretch"
          controlzex:NonClientControlProperties.HitTestResult="CAPTION"
          Background="Transparent">
          <TextBlock
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontWeight="Bold"
            Text="{Binding (local:Dialog.DialogTitle), ElementName=DialogImpl, FallbackValue=Gum}" />
        </Grid>

        <Grid Grid.Row="1">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <Separator Background="{DynamicResource Frb.Brushes.Contrast01}" />
          <Border Grid.Row="1">
            <local:Dialog x:Name="DialogImpl" />
          </Border>
        </Grid>

      </Grid>
    </Border>

  </Border>

</controlzex:WindowChromeWindow>
