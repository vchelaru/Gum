<UserControl
  x:Class="Gum.Services.Dialogs.PluginsDialogView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Gum.Services.Dialogs"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  d:DesignHeight="450"
  d:DesignWidth="450"
  Width="450" Height="450"
  local:Dialog.DialogTitle="{Binding Title}"
  mc:Ignorable="d">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Plugins}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <CheckBox
              Margin="4,4,4,0"
              IsChecked="{Binding IsEnabled, Mode=TwoWay}"
              Content="{Binding DisplayText}" />
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <!-- Says which folder was searched and what was in it, so a plugin missing from the list above
         can be told apart from one that was never installed. Read-only but selectable, since the
         useful thing to do with it is paste it into a bug report. -->
    <Expander
      Grid.Row="1"
      Margin="4,8,4,4"
      Header="Plugin folder scan">
      <TextBox
        MaxHeight="160"
        Margin="0,4,0,0"
        FontFamily="Consolas"
        HorizontalScrollBarVisibility="Auto"
        IsReadOnly="True"
        Text="{Binding Diagnostics, Mode=OneWay}"
        TextWrapping="Wrap"
        VerticalScrollBarVisibility="Auto" />
    </Expander>
  </Grid>
</UserControl>
