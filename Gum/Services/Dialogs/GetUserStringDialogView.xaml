<UserControl
    x:Class="Gum.Services.Dialogs.GetUserStringDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Gum.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Gum.Services.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="clr-namespace:Gum.Mvvm"
    Width="300"
    d:DataContext="{d:DesignInstance Type={x:Type local:GetUserStringDialogBaseViewModel},
                                     IsDesignTimeCreatable=False}"
    local:Dialog.DialogTitle="{Binding Title}"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Padding="0,0,4,0"
                VerticalAlignment="Center"
                Text="{Binding Prefix}"
                Visibility="{Binding Prefix, Converter={StaticResource NullToVisibilityConverter}}" />
            <TextBox
                x:Name="ValueTextBox"
                Grid.Column="1"
                Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>
        <TextBlock
            Margin="0,4,0,0"
            Text="{Binding Error}"
            TextWrapping="Wrap"
            Visibility="{Binding Error, Converter={StaticResource NullToVisibilityConverter}}" />
    </StackPanel>
</UserControl>
