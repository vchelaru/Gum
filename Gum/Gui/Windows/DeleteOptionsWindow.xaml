<Window
    x:Class="Gum.Gui.Windows.DeleteOptionsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Gum.Gui.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    Title="Confirm Delete"
    Width="350"
    Height="Auto"
    MinHeight="60"
    AllowsTransparency="True"
    Background="Transparent"
    KeyDown="HandleKeyDown"
    ResizeMode="CanResize"
    SizeToContent="WidthAndHeight"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None"
    mc:Ignorable="d">

    <Border
        Background="{DynamicResource Frb.Surface01}"
        BorderBrush="{StaticResource Frb.Brushes.Border}"
        BorderThickness="1"
        CornerRadius="8"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
        <Border>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="24" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontWeight="Bold"
                    Text="{Binding Title, ElementName=Root}" />
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Separator Background="{DynamicResource Frb.Brushes.Contrast01}" />
                    <Border Grid.Row="1" Padding="12">
                        <Grid Height="Auto" Margin="3">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Label x:Name="LabelInstance">Are you sure you want to delete {0}?</Label>
                            <StackPanel x:Name="StackPanelInstance" Grid.Row="1" />
                            <StackPanel
                                Margin="0,12,0,0"
                                Grid.Row="2"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Orientation="Horizontal">
                                <Button Width="70" Click="YesButtonClick" Margin="0,0,6,0">
                                    <AccessText>_Yes</AccessText>
                                </Button>
                                <Button Width="70" Click="NoButtonClick" >
                                    <AccessText>_No</AccessText>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

            </Grid>
        </Border>

    </Border>

</Window>
