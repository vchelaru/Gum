<!--
    Styles for the element tree (GumTreeView).

    This replaces MultiSelectTreeView.Theming.cs, which drew every row by hand in GDI+ - row
    background, hover fill, selection outline, chevrons, icon and text - because a WinForms TreeView
    offers no other way to theme itself. None of that survives here: a row is a template, and the
    states are triggers.

    Selection binds to the node model's IsSelected rather than TreeViewItem.IsSelected, because
    TreeView enforces a single selected item and would clear the previous one on every change. See
    GumTreeView for the whole story.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Gum.Controls"
    xmlns:managers="clr-namespace:Gum.Managers">

    <ResourceDictionary.MergedDictionaries>
        <!-- BoolToVisibilityHiddenConverter, used by the row's expander, is resolved at parse time. -->
        <ResourceDictionary Source="pack://application:,,,/Gum;component/Themes/Frb.Converters.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--
        One row icon: the artwork as an opacity mask over the icon's theme color. The source PNGs
        are white-on-transparent with alpha carrying the shading, so multiplying by a color is what
        "tinting" means for them.
    -->
    <Style TargetType="{x:Type controls:TreeNodeIcon}">
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="16" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsHitTestVisible" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:TreeNodeIcon}">
                    <Rectangle
                        Fill="{Binding IconBrush, RelativeSource={RelativeSource TemplatedParent}}"
                        OpacityMask="{Binding IconMask, RelativeSource={RelativeSource TemplatedParent}}"
                        SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- The expand/collapse chevron. Drawn as geometry so it scales with the UI font. -->
    <Style x:Key="Frb.TreeView.ExpanderToggle" TargetType="{x:Type ToggleButton}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Width" Value="14" />
        <Setter Property="Height" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Background="Transparent" Padding="2">
                        <Path
                            x:Name="Chevron"
                            Data="M 0 0 L 4 4 L 0 8"
                            Stroke="{DynamicResource Frb.Brushes.Foreground}"
                            StrokeThickness="1.5"
                            StrokeStartLineCap="Round"
                            StrokeEndLineCap="Round"
                            StrokeLineJoin="Round"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            RenderTransformOrigin="0.5,0.5" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Chevron" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="90" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        A row. The expander slot is always reserved (not collapsed) so sibling labels line up
        whether or not they have children - the WinForms tree reserved the same space via Indent.
    -->
    <Style x:Key="Frb.TreeView.ItemStyle" TargetType="{x:Type TreeViewItem}">
        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                    <StackPanel>
                        <Border
                            x:Name="RowBorder"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            CornerRadius="2"
                            SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <ToggleButton
                                    x:Name="Expander"
                                    Grid.Column="0"
                                    Style="{StaticResource Frb.TreeView.ExpanderToggle}"
                                    IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                    Visibility="{Binding HasChildren, Converter={StaticResource BoolToVisibilityHiddenConverter}}" />

                                <controls:TreeNodeIcon
                                    Grid.Column="1"
                                    Margin="2,0,4,0"
                                    ImageIndex="{Binding ImageIndex}"
                                    Width="{Binding IconSize, RelativeSource={RelativeSource AncestorType={x:Type controls:GumTreeView}}}"
                                    Height="{Binding IconSize, RelativeSource={RelativeSource AncestorType={x:Type controls:GumTreeView}}}" />

                                <TextBlock
                                    Grid.Column="2"
                                    Text="{Binding Text}"
                                    VerticalAlignment="Center"
                                    TextTrimming="None"
                                    Foreground="{DynamicResource Frb.Brushes.Foreground}" />
                            </Grid>
                        </Border>

                        <ItemsPresenter x:Name="ItemsHost" Margin="14,0,0,0" />
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <!--
                            Hover and selection read the node model, not the container, because the
                            container's own IsSelected is deliberately never set.
                        -->
                        <DataTrigger Binding="{Binding IsHot}" Value="True">
                            <Setter TargetName="RowBorder" Property="Background"
                                    Value="{DynamicResource Frb.Brushes.Primary.Transparent}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter TargetName="RowBorder" Property="Background"
                                    Value="{DynamicResource Frb.Brushes.Primary.Transparent}" />
                            <Setter TargetName="RowBorder" Property="BorderBrush"
                                    Value="{DynamicResource Frb.Brushes.Primary}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:GumTreeView}">
        <Setter Property="Background" Value="{DynamicResource Frb.Surface01}" />
        <Setter Property="Foreground" Value="{DynamicResource Frb.Brushes.Foreground}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource Frb.TreeView.ItemStyle}" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <!--
                    ItemContainerStyle is set here as well as on the TreeView: the TreeView's own
                    setter reaches root rows only, and nested rows would otherwise fall back to the
                    default TreeViewItem style.
                -->
                <HierarchicalDataTemplate
                    DataType="{x:Type managers:GumTreeNode}"
                    ItemsSource="{Binding Nodes}"
                    ItemContainerStyle="{StaticResource Frb.TreeView.ItemStyle}" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:GumTreeView}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer
                            x:Name="PART_ScrollViewer"
                            Focusable="False"
                            CanContentScroll="False"
                            Padding="{TemplateBinding Padding}"
                            HorizontalScrollBarVisibility="Auto"
                            VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter SnapsToDevicePixels="True" />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
