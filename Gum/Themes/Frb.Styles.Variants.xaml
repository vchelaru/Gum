<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="pack://application:,,,/Gum;component/Themes/Frb.Theming.xaml" />
    <ResourceDictionary Source="pack://application:,,,/Gum;component/Themes/Frb.Styles.Defaults.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <Style
    x:Key="Frb.Styles.Textbox.Readonly"
    BasedOn="{StaticResource {x:Type TextBox}}"
    TargetType="TextBox">

    <Style.Resources>
      <SolidColorBrush x:Key="MaterialDesign.Brush.TextBox.HoverBorder" Color="Transparent" />
    </Style.Resources>
    <Setter Property="IsReadOnly" Value="True" />
    <Setter Property="materialDesign:TextFieldAssist.UnderlineBrush" Value="Transparent" />
    <Setter Property="Background" Value="{DynamicResource Frb.Brushes.Background}" />
  </Style>

  <Style
    x:Key="Frb.Styles.TextBlock.Caption"
    BasedOn="{StaticResource {x:Type TextBlock}}"
    TargetType="{x:Type TextBlock}">
    <Setter Property="FontSize" Value="{Binding Source={StaticResource Scale}, Path=Caption}" />
  </Style>
  <Style
    x:Key="Frb.Styles.TextBlock.H2"
    BasedOn="{StaticResource {x:Type TextBlock}}"
    TargetType="{x:Type TextBlock}">
    <Setter Property="FontSize" Value="{Binding Source={StaticResource Scale}, Path=H2}" />
  </Style>


</ResourceDictionary>