// This project's only purpose is to prove the checked-in generated code (Screens/) compiles
// against SkiaGum via a real dotnet build (see .github/workflows/build-and-test.yaml's
// "Build Generated Code Projects (Windows Only)" step) -- it is never run.
return;
