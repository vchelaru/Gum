<Project Sdk="Microsoft.NET.Sdk">
  <!--
    Standalone fixture assembly for IsolatedPluginHostTests (issue #4723): proves that a plugin
    assembly loaded through IsolatedPluginHost gets its own independent static state, separate
    from the same assembly's copy already loaded into the default AssemblyLoadContext. Not added
    to Gum.slnx - built transitively via ProjectReference, same as CrossAssemblyAttributeFixture.
  -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>
</Project>
