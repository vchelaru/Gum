# Prerequisites for the Bubblegum Forms theme.
# Read by GumFormsPlugin.Services.ThemeRequirements at import time.
# This file describes project-level edits only — runtime concerns
# (NuGet packages on the user's game project) are not the tool's job.
# Unknown keys are ignored (forward-compatible).

# Bubblegum sizes fonts dynamically at runtime; BMFont can't produce
# equivalent output. Switching re-rasterizes every font in the project.
FontGenerator: KernSmith

# Bubblegum uses Skia-backed Standards (RoundedRectangle, ColoredCircle)
# for its chrome. The apply step adds the full Skia shape bundle —
# Arc, Canvas, ColoredCircle, Line, LottieAnimation, RoundedRectangle, Svg —
# since they ship as a unit.
RequiresSkiaShapes: true
