namespace WpfDataUi.Controls
{
    /// <summary>The unit an angle displayer writes to its member; it always shows degrees.</summary>
    public enum AngleType
    {
        Degrees,
        Radians
    }
}
